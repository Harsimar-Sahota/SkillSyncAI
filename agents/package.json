{
  "name": "agents",
  "version": "1.0.0",
  "description": "AI Agents for SkillSyncAI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.4", // For API calls within agents
    "dotenv": "^16.5.0", // To load API keys if needed directly
    "openai": "^4.93.0", // Or Azure OpenAI SDK
    "pdf-parse": "^1.1.1",
    "puppeteer": "^24.6.1" // For LinkedIn scraping (use carefully)
    // Add Semantic Kernel or other agent frameworks if planned
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@types/pdf-parse": "^1.1.5",
    "typescript": "^5.8.3"
  }
} 